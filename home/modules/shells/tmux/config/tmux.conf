
set -g @dracula-plugins "battery cpu-usage gpu-usage ram-usage weather"
set -g @dracula-show-fahrenheit false
set-option -g xterm-keys on
set -s extended-keys on
set -as terminal-features 'xterm*:extkeys'
